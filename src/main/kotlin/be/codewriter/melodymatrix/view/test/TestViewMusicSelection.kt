package be.codewriter.melodymatrix.view.test

import javafx.scene.layout.VBox

class TestViewMusicSelection(midiSimulator: MidiSimulator) : VBox() {

    init {
        spacing = 10.0

        children.setAll(

        )
    }
}
